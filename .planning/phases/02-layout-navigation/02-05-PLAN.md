---
phase: 02-layout-navigation
plan: 05
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified: [src/components/layout/Layout/Layout.tsx, src/components/layout/Layout/index.ts, src/App.tsx, src/index.css]
autonomous: true

must_haves:
  truths:
    - "Header appears fixed at top of page"
    - "Footer appears at bottom of page"
    - "Mobile bottom nav appears on mobile, hidden on desktop"
    - "Section spacing is 120-160px between sections"
    - "Sections are min-100vh"
  artifacts:
    - path: "src/components/layout/Layout/Layout.tsx"
      provides: "Main layout container with Header, Footer, children"
      min_lines: 30
    - path: "src/App.tsx"
      provides: "App root with Layout and placeholder sections"
      contains: "Layout"
    - path: "src/index.css"
      provides: "Section spacing CSS utilities"
      contains: "section-spacing"
  key_links:
    - from: "src/components/layout/Layout/Layout.tsx"
      to: "src/components/layout/Header/index.ts"
      via: "import Header"
      pattern: "from.*@components/layout/Header"
    - from: "src/components/layout/Layout/Layout.tsx"
      to: "src/components/layout/Footer/index.ts"
      via: "import Footer"
      pattern: "from.*@components/layout/Footer"
    - from: "src/components/layout/Layout/Layout.tsx"
      to: "src/components/layout/MobileNav/index.ts"
      via: "import MobileNav"
      pattern: "from.*@components/layout/MobileNav"
---

<objective>
Create main layout container and integrate Header, Footer, and MobileNav with placeholder sections for testing.

Purpose: Assembles all layout components into a cohesive page structure with proper section spacing, enabling content sections to be added in future phases.

Output: Working Layout component integrated into App.tsx with all navigation elements and placeholder content sections.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-layout-navigation/02-CONTEXT.md
@.planning/phases/02-layout-navigation/02-RESEARCH.md
@.planning/phases/02-layout-navigation/02-01-SUMMARY.md
@.planning/phases/02-layout-navigation/02-02-SUMMARY.md
@.planning/phases/02-layout-navigation/02-03-SUMMARY.md
@.planning/phases/02-layout-navigation/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Add section spacing utilities to index.css</name>
  <files>src/index.css</files>
  <action>
    Add section spacing utilities at end of file in @layer components:

    @layer components {
      .safe-bottom {
        padding-bottom: env(safe-area-inset-bottom, 20px);
      }

      .section {
        min-height: 100vh;
        padding-top: 120px;
        padding-bottom: 120px;
      }

      .section-spacing {
        padding-top: 160px;
        padding-bottom: 160px;
      }
    }

    Section utility classes:
    - .section: min-height 100vh with 120px padding (default)
    - .section-spacing: 160px padding for generous spacing between sections
    Follows CONTEXT.md requirement of 120-160px between sections.
  </action>
  <verify>grep -q "section-spacing" src/index.css</verify>
  <done>Section spacing utility classes added</done>
</task>

<task type="auto">
  <name>Create Layout.tsx main container</name>
  <files>src/components/layout/Layout/Layout.tsx</files>
  <action>
    Create main layout container component:

    import type React from "react";
    import { Header } from "../Header";
    import { Footer } from "../Footer";
    import { MobileNav } from "../MobileNav";

    type LayoutProps = {
      children: React.ReactNode;
    };

    export const Layout: React.FC<LayoutProps> = ({ children }) => {
      return (
        <div className="min-h-screen">
          <Header>
            <Header.Logo />
            <Header.Navigation />
          </Header>
          <main className="pt-20">
            {children}
          </main>
          <Footer />
          <MobileNav />
        </div>
      );
    };

    Layout includes:
    - Fixed Header with Logo and Navigation
    - Main content area with top padding (pt-20) to account for fixed header
    - Footer at bottom
    - MobileNav for mobile devices

    pt-20 (80px) matches the header's scroll-padding-top from plan 02-03.
  </action>
  <verify>file exists and imports all layout components</verify>
  <done>Layout container component created</done>
</task>

<task type="auto">
  <name>Create index.ts for Layout</name>
  <files>src/components/layout/Layout/index.ts</files>
  <action>
    Create assembly file following CLAUDE.md Composition Pattern:

    import { Layout } from "./Layout";

    export { Layout };

    Simple component with no sub-parts to attach.
  </action>
  <verify>file exists and exports Layout</verify>
  <done>Layout properly exported</done>
</task>

<task type="auto">
  <name>Create test file for Layout</name>
  <files>src/components/layout/Layout/__tests__/Layout.test.tsx</files>
  <action>
    Create test file following project conventions:

    import { render, screen } from "@testing-library/react";
    import { describe, expect, it } from "vitest";
    import { Layout } from "../Layout";

    describe("Layout", () => {
      it("should render header, main, and footer", () => {
        render(
          <Layout>
            <div>Test Content</div>
          </Layout>
        );
        expect(screen.getByRole("banner")).toBeInTheDocument(); // header
        expect(screen.getByRole("main")).toBeInTheDocument(); // main
        expect(screen.getByRole("contentinfo")).toBeInTheDocument(); // footer
      });

      it("should render children in main area", () => {
        render(
          <Layout>
            <div>Test Content</div>
          </Layout>
        );
        expect(screen.getByText("Test Content")).toBeInTheDocument();
      });
    });

    Tests verify layout structure and children rendering.
  </action>
  <verify>npm run test -- --run Layout.test.tsx passes</verify>
  <done>Layout tests created and passing</done>
</task>

<task type="auto">
  <name>Update App.tsx with Layout and placeholder sections</name>
  <files>src/App.tsx</files>
  <action>
    Update App.tsx to use Layout and add placeholder sections for testing navigation:

    import { Layout } from "@components/layout/Layout";
    import { MobileNavProvider } from "@components/layout/MobileNav";

    function App() {
      return (
        <MobileNavProvider>
          <Layout>
            <section id="about" className="section">
              <div className="container mx-auto px-4">
                <h1 className="text-4xl font-bold">About</h1>
                <p className="mt-4 text-muted-foreground">
                  Professional background and positioning statement will go here.
                </p>
              </div>
            </section>

            <section id="career" className="section">
              <div className="container mx-auto px-4">
                <h1 className="text-4xl font-bold">Career</h1>
                <p className="mt-4 text-muted-foreground">
                  Career timeline will go here.
                </p>
              </div>
            </section>

            <section id="skills" className="section">
              <div className="container mx-auto px-4">
                <h1 className="text-4xl font-bold">Skills</h1>
                <p className="mt-4 text-muted-foreground">
                  Technical skills and technologies will go here.
                </p>
              </div>
            </section>

            <section id="projects" className="section">
              <div className="container mx-auto px-4">
                <h1 className="text-4xl font-bold">Projects</h1>
                <p className="mt-4 text-muted-foreground">
                  Work projects will go here.
                </p>
              </div>
            </section>

            <section id="contact" className="section">
              <div className="container mx-auto px-4">
                <h1 className="text-4xl font-bold">Contact</h1>
                <p className="mt-4 text-muted-foreground">
                  Contact information will go here.
                </p>
              </div>
            </section>
          </Layout>
        </MobileNavProvider>
      );
    }

    export default App;

    Each section has:
    - Matching id attribute for navigation links
    - .section class for min-height and spacing
    - Placeholder content for testing navigation
    - container for centering content

    Wrapped in MobileNavProvider for active section tracking.
  </action>
  <verify>grep -q "id=\"about\"" src/App.tsx && grep -q "id=\"contact\"" src/App.tsx</verify>
  <done>App.tsx updated with Layout and placeholder sections</done>
</task>

</tasks>

<verification>
- npm run build completes without errors
- npm run lint passes
- npm run test -- --run passes for all Layout tests
- App renders with Header at top, Footer at bottom
- Desktop navigation links work and smooth scroll to sections
- Mobile bottom nav appears on mobile viewport
- Active section highlighting works for both desktop and mobile
- Progress indicators work (line on desktop, ring on mobile)
- Section spacing is 120-160px between sections
</verification>

<success_criteria>
Layout component integrated into App.tsx with all navigation elements working. Placeholder sections allow testing navigation. All tests passing. Phase 02 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/02-layout-navigation/02-05-SUMMARY.md`
</output>
