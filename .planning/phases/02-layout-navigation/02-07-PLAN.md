---
phase: 02-layout-navigation
plan: 07
type: execute
wave: 3
depends_on: [02-02, 02-03]
files_modified: [src/components/layout/Layout/Layout.tsx, src/components/layout/Layout/index.ts, src/components/layout/Layout/__tests__/Layout.test.tsx]
autonomous: true

must_haves:
  truths:
    - "Layout component renders Header with Logo and Navigation"
    - "Layout component renders Footer"
    - "Layout component renders children in main element"
    - "Main element has padding to account for fixed header"
  artifacts:
    - path: "src/components/layout/Layout/Layout.tsx"
      provides: "Main layout container with Header, Footer, children"
      min_lines: 30
    - path: "src/components/layout/Layout/index.ts"
      provides: "Layout export"
      exports: ["Layout"]
  key_links:
    - from: "src/components/layout/Layout/Layout.tsx"
      to: "src/components/layout/Header/index.ts"
      via: "import Header"
      pattern: "from.*@components/layout/Header"
    - from: "src/components/layout/Layout/Layout.tsx"
      to: "src/components/layout/Footer/index.ts"
      via: "import Footer"
      pattern: "from.*@components/layout/Footer"
---

<objective>
Create main layout container component that integrates Header, Footer, and main content area.

Purpose: Assembles all layout components into a cohesive page structure. The Layout component is the container that wraps all page content with consistent navigation elements.

Output: Working Layout component with Header, Footer, and main content area.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-layout-navigation/02-CONTEXT.md
@.planning/phases/02-layout-navigation/02-RESEARCH.md
@.planning/phases/02-layout-navigation/02-02-SUMMARY.md
@.planning/phases/02-layout-navigation/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Create Layout.tsx main container</name>
  <files>src/components/layout/Layout/Layout.tsx</files>
  <action>
    Create main layout container component:

    import type React from "react";
    import { Header } from "../Header";
    import { Footer } from "../Footer";

    type LayoutProps = {
      children: React.ReactNode;
    };

    export const Layout: React.FC<LayoutProps> = ({ children }) => {
      return (
        <div className="min-h-screen">
          <Header>
            <Header.Logo />
            <Header.Navigation />
          </Header>
          <main className="pt-20">
            {children}
          </main>
          <Footer />
        </div>
      );
    };

    Layout includes:
    - Fixed Header with Logo and Navigation
    - Main content area with top padding (pt-20) to account for fixed header
    - Footer at bottom

    pt-20 (80px) matches the header's scroll-padding-top from plan 02-04.
    MobileNav will be added directly in App.tsx (needs MobileNavProvider wrapper).
  </action>
  <verify>file exists and imports Header and Footer</verify>
  <done>Layout container component created</done>
</task>

<task type="auto">
  <name>Create index.ts for Layout</name>
  <files>src/components/layout/Layout/index.ts</files>
  <action>
    Create assembly file following CLAUDE.md Composition Pattern:

    import { Layout } from "./Layout";

    export { Layout };

    Simple component with no sub-parts to attach.
  </action>
  <verify>file exists and exports Layout</verify>
  <done>Layout properly exported</done>
</task>

<task type="auto">
  <name>Create test file for Layout</name>
  <files>src/components/layout/Layout/__tests__/Layout.test.tsx</files>
  <action>
    Create test file following project conventions:

    import { render, screen } from "@testing-library/react";
    import { describe, expect, it } from "vitest";
    import { Layout } from "../Layout";

    describe("Layout", () => {
      it("should render header, main, and footer", () => {
        render(
          <Layout>
            <div>Test Content</div>
          </Layout>
        );
        expect(screen.getByRole("banner")).toBeInTheDocument(); // header
        expect(screen.getByRole("main")).toBeInTheDocument(); // main
        expect(screen.getByRole("contentinfo")).toBeInTheDocument(); // footer
      });

      it("should render children in main area", () => {
        render(
          <Layout>
            <div>Test Content</div>
          </Layout>
        );
        expect(screen.getByText("Test Content")).toBeInTheDocument();
      });
    });

    Tests verify layout structure and children rendering.
  </action>
  <verify>npm run test -- --run Layout.test.tsx passes</verify>
  <done>Layout tests created and passing</done>
</task>

</tasks>

<verification>
- npm run build completes without errors
- npm run lint passes
- npm run test -- --run passes for Layout tests
- Layout component can be imported from @components/layout/Layout
- Layout renders Header with Logo and Navigation
- Layout renders Footer
- Layout renders children in main element
- Main element has pt-20 class for header clearance
</verification>

<success_criteria>
Layout component created with Header, Footer, and main content area. All tests passing. Ready for integration into App.tsx.
</success_criteria>

<output>
After completion, create `.planning/phases/02-layout-navigation/02-07-SUMMARY.md`
</output>
