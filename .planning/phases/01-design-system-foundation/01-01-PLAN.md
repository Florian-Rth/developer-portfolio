---
phase: 01-design-system-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, components.json, src/lib/utils.ts]
autonomous: true

must_haves:
  truths:
    - "shadcn CLI runs successfully and installs dependencies"
    - "components.json exists at project root with valid configuration"
    - "Base UI primitives are installed (@base-ui/react in package.json)"
    - "Tailwind CSS v4 Vite plugin is installed (@tailwindcss/vite in package.json)"
    - "clsx and tailwind-merge are installed for cn utility"
    - "src/lib/utils.ts exists with cn() function exported"
    - "components.json uses Nova style, Zinc base color, and HugeIcons"
    - "CSS variables are enabled in components.json"
  artifacts:
    - path: "components.json"
      provides: "shadcn/ui configuration"
      contains: ["style: nova", "baseColor: zinc", "cssVariables: true"]
    - path: "src/lib/utils.ts"
      provides: "cn utility function for className merging"
      exports: ["cn"]
    - path: "package.json"
      provides: "Installed dependencies"
      contains: ["@base-ui/react", "@tailwindcss/vite", "clsx", "tailwind-merge"]
  key_links:
    - from: "src/lib/utils.ts"
      to: "components.json"
      via: "shadcn CLI creates utils.ts based on aliases in config"
      pattern: "cn.*function.*clsx.*twMerge"
---

<objective>
Install shadcn/ui infrastructure using the official CLI, setting up Tailwind CSS v4, Base UI primitives, and base utilities.

Purpose: Establish the foundational tooling that all other phases depend on. The CLI automates dependency installation and creates the configuration file that enables component installation in later phases.

Output: Installed packages (Tailwind v4 plugin, Base UI, clsx, tailwind-merge), components.json configuration file, and src/lib/utils.ts with cn() utility.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system-foundation/01-CONTEXT.md
@.planning/phases/01-design-system-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Run shadcn create CLI</name>
  <files>package.json, components.json, src/lib/utils.ts</files>
  <action>
    Run `npx shadcn@latest create` with the following selections:
    - Framework: Vite
    - Language: TypeScript
    - Style: Nova (user selected in CONTEXT.md)
    - Base color: Zinc (user selected in CONTEXT.md)
    - CSS variables: Yes
    - Icon library: HugeIcons (user selected in CONTEXT.md)

    Accept all defaults for other prompts. The CLI will:
    1. Install @tailwindcss/vite, @base-ui/react, clsx, tailwind-merge
    2. Create components.json at project root
    3. Create src/lib/utils.ts with cn() function
    4. Configure basic aliases

    NOTE: shadcn CLI uses @/ style aliases by default. We will customize to category-based aliases (@components, @lib, etc.) in the next plan (01-02) when configuring Vite.
  </action>
  <verify>Run: ls components.json src/lib/utils.ts && grep -E "@base-ui/react|@tailwindcss/vite|clsx|tailwind-merge" package.json</verify>
  <done>All four files exist and dependencies are listed in package.json</done>
</task>

<task type="auto">
  <name>Install HugeIcons package</name>
  <files>package.json</files>
  <action>
    The shadcn CLI selection of HugeIcons does NOT auto-install the package (unlike Lucide).

    Run: `npm install @hugeicons/react`

    Verify the package was added to package.json.
  </action>
  <verify>grep "@hugeicons/react" package.json</verify>
  <done>@hugeicons/react is listed in package.json dependencies</done>
</task>

<task type="auto">
  <name>Verify components.json configuration</name>
  <files>components.json</files>
  <action>
    Read components.json and verify it contains:
    - "style": "nova"
    - "baseColor": "zinc"
    - "cssVariables": true
    - "iconLibrary": "hugeicons"
    - Aliases section (even if @/ style, we'll customize in next plan)

    If any values are incorrect, manually edit components.json to match. Use the pattern from RESEARCH.md > Pattern 2 as reference.
  </action>
  <verify>grep -E '"style".*"nova".*"baseColor".*"zinc".*"cssVariables".*true' components.json</verify>
  <done>components.json has correct Nova style, Zinc color, CSS variables enabled, and HugeIcons selected</done>
</task>

<task type="auto">
  <name>Verify cn utility function</name>
  <files>src/lib/utils.ts</files>
  <action>
    Read src/lib/utils.ts and verify it exports a `cn` function that:
    - Imports clsx and tailwind-merge
    - Combines inputs using clsx then merges using twMerge
    - Uses proper TypeScript types (ClassValue from clsx)

    Reference: RESEARCH.md > Pattern 6 for expected implementation.
  </action>
  <verify>grep -E "export.*function.*cn.*clsx.*twMerge" src/lib/utils.ts</verify>
  <done>src/lib/utils.ts exports cn() function that combines clsx and twMerge</done>
</task>

</tasks>

<verification>
After all tasks complete, verify the design system foundation is installed:

1. Run `npm run build` to ensure TypeScript compilation works
2. Run `npm run lint` to ensure Biome is happy with new files
3. Check `node_modules` contains: @base-ui/react, @tailwindcss/vite, clsx, tailwind-merge, @hugeicons/react

If build or lint fails, fix issues before proceeding.
</verification>

<success_criteria>
1. `npx shadcn@latest create` completed without errors
2. components.json exists at project root with Nova style, Zinc color, CSS variables enabled
3. @base-ui/react, @tailwindcss/vite, clsx, tailwind-merge, @hugeicons/react are in package.json
4. src/lib/utils.ts exists and exports cn() function
5. `npm run build` completes successfully
6. `npm run lint` completes successfully
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system-foundation/01-01-SUMMARY.md` with:
- What was installed (packages, config files)
- Any deviations from defaults
- Next step (configure Vite with Tailwind plugin and path aliases)
</output>
